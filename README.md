# UIImage Region Selection

This small repository demonstrates how to convert from `UIView` coordinates into image coordinates for the purpose of selecting an image region.

![A screenshot](screenshots/b.png)